// import moment from "moment";
const style = {
    fontSize: "18px",
    color:"rgb(69, 97, 150)",
    
}
const HeaderInfo = props =>{
    // const currentDay = moment(new Date).format('DD/MM/YYYY') 
    return (
        <h1 style={style}>Transfer GROSS to NET and vice versa</h1>
    )
}

export default HeaderInfo